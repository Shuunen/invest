interface:
  display_name: "Invest"
  short_description: "Repo-specific patterns and workflows for invest"
  default_prompt: "Use the invest repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true